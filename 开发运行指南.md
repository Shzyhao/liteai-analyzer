# 轻析 · AI 文件分析助手 — 开发运行指南

## 技术栈
- **Tauri 2.0**（Rust 后端 + React/Vite 前端）
- 核心逻辑拆为 5 个可测试 lib crate（`crates/liteai-*`），零依赖 tauri
- 模型接口：DeepSeek（OpenAI 兼容，SSE 流式）

## 目录结构
```
crates/
  liteai-core    领域模型 + 编排管线（GUI/CLI 共用）
  liteai-parsers 文件解析器（txt/xlsx/docx/pdf）
  liteai-model   DeepSeek 流式客户端（SSE/余额/连通测试）
  liteai-output  Markdown / Word 序列化器
  liteai-config  配置存储 + keyring 密钥 + 模板
  liteai-cli     命令行冒烟工具
src-tauri/       Tauri 壳（命令层 / 右键集成 / 单实例）
src/             React 前端（流式渲染 / 设置 / 模板 / 队列）
samples/         测试样本
```

## 本机工具链（已安装）
- Rust 1.97.1（rustup，MSVC target）
- **VS Build Tools 2022** 安装在 `D:\BuildTools`（VCTools workload）
- cargo 走清华镜像（`~/.cargo/config.toml`），MSVC 链接器已全局配置

> 关键：必须让 MSVC 的 `link.exe` 优先于 Git 的 GNU `link.exe`，否则链接报 "extra operand"。
> `~/.cargo/config.toml` 中已通过 `[target.x86_64-pc-windows-msvc] linker` 固定。

## 常用命令

```bash
# 运行核心库测试（22 项）
cargo test -p liteai-core -p liteai-parsers -p liteai-model -p liteai-output -p liteai-config

# CLI 端到端冒烟（需 LITEAI_API_KEY 环境变量）
LITEAI_API_KEY=sk-xxx cargo run -p liteai-cli -- analyze samples/项目周报.txt --docx

# 开发模式（GUI）
npm run tauri dev

# 打包（便携版 + 安装版）
npm run tauri build
```

## GUI 使用流程
1. `npm run tauri dev` 启动
2. 设置页填入 Base URL（`https://api.deepseek.com`）、模型（`deepseek-chat`）、API Key
3. 点击「测试连接 & 查余额」验证
4. 右键菜单页点「安装右键菜单」
5. 在资源管理器选中文件 → 右键 →「AI 分析」

## 待办
- [ ] Win11 新版右键菜单原生入口（当前走"显示更多选项"）
- [ ] 托盘通知、无痕模式
- [ ] 企业版模块（集中下发/审计/网关拦截）
