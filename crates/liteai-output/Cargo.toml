[package]
name = "liteai-output"
version.workspace = true
edition.workspace = true
description = "AI 文件分析助手 · 结果序列化器（Markdown / Word）"

[dependencies]
liteai-core = { path = "../liteai-core" }
docx-rs = "0.4"
rust_xlsxwriter = "0.77"

[dev-dependencies]
tempfile = "3"
