# 更新日志 / Changelog

## v0.2.0（开发版，待发布）

### ✨ 新功能

#### 多套 API 配置管理
- 可配置多套 API（Base URL + 模型 + Key），**同一时刻只使用一套**
- 列表一键「设为当前」切换，无需修改
- 每套配置的 Key 独立保存在 Windows 凭据管理器，互不干扰
- 可新增 / 编辑 / 删除配置，删除时 Key 一并清除
- 旧版单一配置**自动迁移**，无需重新配置

#### 导出技能（自定义脚本导出）
- 「导出技能」页管理自定义导出脚本（PowerShell / Python / Node / bat）
- 分析完成后在结果卡片点「自定义导出 ▾」选择技能运行
- 脚本接收：结果文件路径（末位参数）+ 环境变量 `LITEAI_SOURCE_FILE` / `LITEAI_ANALYSIS_FILE` / `LITEAI_OUTPUT_DIR`
- **AI 生成技能**：一句话描述，AI 用严格安全提示词生成脚本（只读入结果 + 写输出文件，禁止网络/删除/系统操作）
- 技能目录默认 `桌面\liteai-skills`（自动创建），**每个技能独立子文件夹**；自定义路径必须已存在否则阻止保存/生成

#### 分析历史记录
- 每次分析自动保存（文件 / 时间 / 模板 / 结果 / Token 用量 / 输出文件）
- 「历史」页：回看结果、一键重新分析、定位文件、删除单条、清空
- 本地存储，上限 50 条

#### 原生 Excel 导出
- 设置勾选「额外导出 Excel」→ 分析自动生成 `.xlsx`（Sheet「分析结果」+「原文」）
- 纯 Rust 生成，**不依赖对方电脑装 Python / Excel**

### 🎨 界面与体验
- **深色模式**（跟随系统 / 浅色 / 深色可切换，顶栏一键切换）
- **流式打字机光标**，生成过程观感更专业
- **结果卡片增强**：打开文件夹 / 打开结果文件 / 显示 Token 用量 / 失败与取消可单独重试
- **拖拽文件进窗口**直接分析
- **首次使用三步引导**（配 Key → 装右键菜单 → 使用）
- **取消时保留已生成内容**，不浪费
- 分析与历史页**空状态提示**
- 各页面**统一内容宽度**并对齐

### 🔧 兼容与稳健性
- **余额查询平台通用化**：DeepSeek / Kimi 查余额，其他平台优雅降级显示「不支持」，**任何情况下不阻塞测试连接**
- Token 用量统计为 OpenAI 兼容标准字段，所有平台通用
- 右键菜单安装 / 卸载 / 重新安装（含状态显示与确认）
- 右键菜单图标修复

---

## v0.1.0（已发布）

- Windows 右键菜单 AI 文件分析（个人版 MVP）
- 右键一键分析 / 批量队列 / DeepSeek SSE 流式输出
- 双通道呈现：UI 实时预览 + `.ai.md` / `.docx` 导出
- 5 个内置 Prompt 模板 + 自定义 + JSON 导入导出
- 文件类型白名单、输出方式记忆（仅UI / 仅文件 / 双开）
- 单实例运行、隐私优先（Key 存凭据管理器，一键清除）
